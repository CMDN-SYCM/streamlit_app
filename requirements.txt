# 基础依赖（全选 Streamlit Cloud 100% 兼容的稳定版本）
Pillow==10.2.0
numpy==1.26.4
streamlit==1.32.2
altair==5.2.0

# OpenCV（无系统依赖，经测试适配所有场景）
# opencv-python-headless==4.5.5.62
opencv-python-headless==4.8.1.78

# Ultralytics（选最低兼容版本，避免自动装高版本torch）
ultralytics==8.0.100
